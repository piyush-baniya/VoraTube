/// VoraTube's public privacy policy URL.
///
/// ⚠️ PLACEHOLDER — NOT A REAL URL YET.
///
/// Google Play requires a publicly hosted privacy policy. The final policy
/// webpage will be hosted outside this repository and its real address is not
/// known yet. Replace [kPrivacyPolicyUrl] with the single, final public URL
/// once the policy page is live. This constant is the ONLY privacy-policy URL
/// in the codebase — do not hardcode the URL anywhere else.
///
/// TODO(release): replace with the real public privacy-policy URL.
const String kPrivacyPolicyUrl =
    'https://YOUR-PRIVACY-POLICY-DOMAIN/privacy-policy';
